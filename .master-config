# Master Configuration File
# This is the single source of truth for all port configurations
# All scripts and applications should read from this file

# =============================================================================
# PORT CONFIGURATION
# =============================================================================

# Backend Configuration
BACKEND_PORT=4750
BACKEND_HOST=0.0.0.0
BACKEND_RELOAD=true

# Frontend Configuration  
FRONTEND_DEV_PORT=4850
FRONTEND_PROD_PORT=4851
FRONTEND_HOST=0.0.0.0

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# Server IP (auto-detected by scripts)
# SERVER_IP=10.5.216.11

# =============================================================================
# URL CONFIGURATION (derived from ports above)
# =============================================================================

# These will be automatically generated by scripts based on SERVER_IP and ports
# NEXT_PUBLIC_BACKEND_URL=http://${SERVER_IP}:${BACKEND_PORT}
# NEXT_PUBLIC_WS_URL=ws://${SERVER_IP}:${BACKEND_PORT}  
# NEXT_PUBLIC_FRONTEND_URL=http://${SERVER_IP}:${FRONTEND_DEV_PORT}

# =============================================================================
# NOTES
# =============================================================================

# To change ports:
# 1. Update the PORT values above
# 2. Run the respective run scripts - they will automatically update all configs
# 3. No need to manually edit multiple files

# Scripts that read this file:
# - run-backend.command
# - run-frontend.command  
# - run-production-frontend.command
